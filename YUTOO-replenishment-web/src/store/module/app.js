export const state = {
  // 需要keep alive的路由
  cachePage: ['/product/amazon/platProduct'],
  // 需要设置返回按钮的页面 第一个元素是要显示返回按钮的路径  第二元素是返回的路径
  showBackList: [
    ['/product/common/setup/temp', '/product/common/setup/category']
  ]
}